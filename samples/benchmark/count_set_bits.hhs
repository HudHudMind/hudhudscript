function countSetBits(n) {
    let count = 0;
    while (n != 0) {
        count = count + (n % 2);
        n = n / 2;
    }
    return count;
}

let total = 0;
for (let i = 0; i < 100000; i = i + 1) {
    total = total + countSetBits(i);
}
print("total=" + total);
